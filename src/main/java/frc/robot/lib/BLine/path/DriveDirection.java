package frc.robot.lib.BLine.path;

/** Which end of a tank robot leads along the authored path; element order is unchanged. */
public enum DriveDirection {
    /** Drive toward targets with the front of the robot leading. */
    FORWARD,
    /** Drive toward targets with the back of the robot leading. */
    BACKWARD
}
