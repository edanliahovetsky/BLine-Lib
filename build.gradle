plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.github.edanliahovetsky'
version = '0.9.1'

java {
    sourceCompatibility = JavaVersion.VERSION_25
    targetCompatibility = JavaVersion.VERSION_25
    withSourcesJar()
    withJavadocJar()
}

repositories {
    maven {
        url = "${System.getProperty('user.home')}/wpilib/2027_alpha7/maven"
    }
    mavenCentral()
    
    // WPILib Maven repository
    maven {
        url = "https://frcmaven.wpi.edu/artifactory/release/"
    }
}

ext {
    wpiVersion = "2027.0.0-alpha-7"
}

configurations {
    wpilibTestNatives
}

dependencies {
    // WPILib dependencies
    implementation "org.wpilib.wpilibj:wpilibj-java:${wpiVersion}"
    compileOnly "org.wpilib.commandsv2:commandsv2-java:${wpiVersion}"
    compileOnly "org.wpilib:commandsv3-java:${wpiVersion}"
    testImplementation "org.wpilib.commandsv2:commandsv2-java:${wpiVersion}"
    testImplementation "org.wpilib:commandsv3-java:${wpiVersion}"
    implementation "org.wpilib.wpimath:wpimath-java:${wpiVersion}"
    implementation "org.wpilib.wpiutil:wpiutil-java:${wpiVersion}"
    implementation "org.wpilib.wpiunits:wpiunits-java:${wpiVersion}"
    // Required by Field2d/SmartDashboard (NTSendable); wpilibj does not pull it transitively here.
    compileOnly "org.wpilib.ntcore:ntcore-java:${wpiVersion}"
    compileOnly "com.fasterxml.jackson.core:jackson-annotations:2.18.3"
    testCompileOnly "org.wpilib.ntcore:ntcore-java:${wpiVersion}"
    testCompileOnly "com.fasterxml.jackson.core:jackson-annotations:2.18.3"
    
    implementation "org.wpilib.telemetry:telemetry-java:${wpiVersion}"
    compileOnly "org.wpilib.tunables:tunables-java:${wpiVersion}"
    testImplementation "org.wpilib.tunables:tunables-java:${wpiVersion}"

    // JSON Simple for path loading
    implementation "com.googlecode.json-simple:json-simple:1.1.1"

    testImplementation "org.junit.jupiter:junit-jupiter:5.11.4"
    testImplementation "org.wpilib.hal:hal-java:${wpiVersion}"
    testRuntimeOnly "org.wpilib.ntcore:ntcore-java:${wpiVersion}"
    testRuntimeOnly "org.wpilib.wpinet:wpinet-java:${wpiVersion}"
    testRuntimeOnly "com.fasterxml.jackson.core:jackson-databind:2.19.2"
    testRuntimeOnly "io.avaje:avaje-jsonb:3.14"
    testRuntimeOnly "org.junit.platform:junit-platform-launcher:1.11.4"
    testRuntimeOnly "us.hebi.quickbuf:quickbuf-runtime:1.4"
}

def currentOperatingSystem = org.gradle.internal.os.OperatingSystem.current()
def wpilibNativeClassifier = currentOperatingSystem.isMacOsX()
    ? "osxuniversal"
    : currentOperatingSystem.isWindows()
        ? "windowsx86-64"
        : "linuxx86-64"
def wpilibNativeSharedPath = currentOperatingSystem.isMacOsX()
    ? "osx/universal/shared"
    : currentOperatingSystem.isWindows()
        ? "windows/x86-64/shared"
        : "linux/x86-64/shared"
def wpilibTestNativeDir = layout.buildDirectory.dir("wpilib-test-natives")

dependencies {
    wpilibTestNatives "org.wpilib.hal:hal-cpp:${wpiVersion}:${wpilibNativeClassifier}@zip"
    wpilibTestNatives "org.wpilib.datalog:datalog-cpp:${wpiVersion}:${wpilibNativeClassifier}@zip"
    wpilibTestNatives "org.wpilib.wpiutil:wpiutil-cpp:${wpiVersion}:${wpilibNativeClassifier}@zip"
    wpilibTestNatives "org.wpilib.ntcore:ntcore-cpp:${wpiVersion}:${wpilibNativeClassifier}@zip"
    wpilibTestNatives "org.wpilib.wpinet:wpinet-cpp:${wpiVersion}:${wpilibNativeClassifier}@zip"
}

tasks.register('extractWpilibTestNatives', Copy) {
    from {
        configurations.wpilibTestNatives.collect { zipTree(it) }
    }
    into wpilibTestNativeDir
    duplicatesStrategy = DuplicatesStrategy.INCLUDE
}

// Configuration for JitPack
publishing {
    publications {
        maven(MavenPublication) {
            from components.java
            
            groupId = 'com.github.edanliahovetsky'
            artifactId = 'BLine-Lib'
            version = project.version
            
            pom {
                name = 'BLine-Lib'
                description = 'A path following library for FRC robots'
                url = 'https://github.com/edanliahovetsky/BLine-Lib'
                
                licenses {
                    license {
                        name = 'BSD 3-Clause License'
                        url = 'https://opensource.org/licenses/BSD-3-Clause'
                    }
                }
            }
        }
    }
}

// Ensure javadoc doesn't fail the build on warnings
javadoc {
    options.addStringOption('Xdoclint:none', '-quiet')
    failOnError = false
}

// Auto-copy javadoc to docs/ for GitHub Pages hosting
task copyJavadocToDocs(type: Copy) {
    dependsOn javadoc
    from "${buildDir}/docs/javadoc"
    into "${projectDir}/docs"
    // Preserve existing files like GIFs
    duplicatesStrategy = DuplicatesStrategy.INCLUDE
}

// GitHub Pages remains stable-only; compatibility artifacts still include the Javadoc jar.

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

test {
    useJUnitPlatform()
    dependsOn tasks.named('extractWpilibTestNatives')
    dependsOn tasks.named('generatePomFileForMavenPublication')
    jvmArgs '--enable-native-access=ALL-UNNAMED',
        '--add-opens=java.base/jdk.internal.vm=ALL-UNNAMED',
        '--add-opens=java.base/java.lang=ALL-UNNAMED'
    systemProperty 'java.library.path', "${layout.buildDirectory.get().asFile}/wpilib-test-natives/${wpilibNativeSharedPath}"
}
